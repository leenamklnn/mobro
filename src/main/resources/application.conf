########################################
# Web server configuration             #
########################################
webserver {
  # The IP address/host the server is bound to. Default is local.
  bindAddress = "0.0.0.0"

  # The port the server listens to. Default is 8089.
  bindPort = 8089

}